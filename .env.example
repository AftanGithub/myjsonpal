# Public environment variables (inlined into the client bundle at build time).
# Copy this file to `.env` and fill in your values, then rebuild.

# Google AdSense publisher ID (optional; ads only render when SITE.showAds is true).
PUBLIC_ADSENSE_CLIENT=
